@model Biblioteca_MVC.ViewModels.MemberBookLoansViewModel

@{
    ViewData["Title"] = "DetailsWithBookLoans";
}

<h1>DetailsWithBookLoans</h1>

<div>
    <h4>MemberBookLoansViewModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NumeMembru)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NumeMembru)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
<label asp-for="BookLoans" class="control-label">Book Loans for this member</label>
@foreach(var item in Model.BookLoans) 
{
    <div style=" border:1px solid red:">
        <p>@Html.DisplayFor(modelItem =>item.IDCarte) </p>
       @* <p>@Html.DisplayFor(modelItem =>item.ContentCode)</p>
        <p>@Html.DisplayFor(modelItem =>item.Revision)</p>*@

    </div>
}
<p>@Html.ActionLink("Back to List", "Index")</p>
